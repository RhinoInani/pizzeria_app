import 'package:flutter/material.dart';
import 'package:pizzeria_app/screens/login/initializeApp.dart';

class loginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: initializeApp(),
    );
  }
}
